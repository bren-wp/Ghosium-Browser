# Ghosium Browser

Ghosium Browser is a privacy-oriented Windows x64 browser distributed by **Brendigo**. Version **0.8.0** focuses on a Ghosium-only product experience, multilingual installation, lower background network activity, weak-PC optimization, Ghosium Search and the Ghosium Store catalog.

## Product identity

All Ghosium-controlled user-facing surfaces use the **Ghosium** brand. Product links generated by Ghosium point only to Ghosium-owned domains:

- https://ghosium.com/
- https://search.ghosium.com/
- https://store.ghosium.com/
- https://ghosium.com/legal/terms
- https://ghosium.com/legal/privacy-policy
- https://ghosium.com/legal/licenses
- https://ghosium.com/support
- https://ghosium.com/security

Required third-party notices remain isolated in `THIRD_PARTY_NOTICES.md` and installed license material. They are legal attribution, not product branding.

## Ghosium internal pages

The source-built Ghosium product uses the `ghost://` namespace for browser-owned internal pages:

- `ghost://newtab/`
- `ghost://history/`
- `ghost://bookmarks/`
- `ghost://downloads/`
- `ghost://settings/`
- `ghost://profiles/`
- `ghost://extensions/`
- `ghost://passwords/`

`ghost://profiles/` routes to Ghosium profile settings and `ghost://passwords/` routes to the Ghosium password manager. The source branding pipeline also uses `ghost-untrusted://` for the corresponding restricted internal WebUI namespace.

## Desktop architecture

The Ghosium-owned Windows launcher is written in **C++20**. There is no Tauri application wrapper, WebView2 browser shell, Rust browser core or TypeScript/Vite browser runtime.

```text
Ghosium-Browser.exe
  -> runtime/Ghosium-Engine.exe
  -> extension/            Ghosium Privacy + New Tab
  -> search-provider/      Ghosium Search integration
```

HTML, CSS and JSON under the bundled components are presentation/configuration resources and contain no custom browser JavaScript runtime.

## Ghosium Search

New-tab and default search entry points use:

```text
https://search.ghosium.com/?q={searchTerms}
```

The source for the database-free shared-hosting service is included in `search-web/`. It uses PHP and `storage/data/*.json` rather than a SQL database.

## Ghosium Store

The source for `https://store.ghosium.com/` is included in `store-web/`.

The Store is a privacy-focused curated catalog with a JSON API and no database, analytics, remote fonts or third-party assets. Automatic public one-click extension installation is enabled only through reviewed engine integration that preserves package trust and extension security.

## Languages

The Setup installer includes **30 languages**. English is the primary/default fallback and Croatian is included.

Supported installer/browser launch locales:

- English
- Croatian
- German
- French
- Spanish
- Italian
- Portuguese
- Portuguese (Brazil)
- Dutch
- Polish
- Czech
- Slovak
- Slovenian
- Hungarian
- Romanian
- Bulgarian
- Greek
- Turkish
- Russian
- Ukrainian
- Swedish
- Danish
- Norwegian
- Finnish
- Japanese
- Korean
- Simplified Chinese
- Traditional Chinese
- Arabic
- Hebrew

The selected installer language is stored in `ghosium-language.txt`; the Ghosium launcher validates the locale and starts the browser using that language. Portable mode asks for a language on first use and stores it beside the portable profile.

## Privacy and security defaults

Ghosium disables browser sync, crash reporting, background browser mode, unnecessary background networking, Domain Reliability reporting and hyperlink-auditing pings at launcher level. The bundled declarative privacy rules block selected third-party trackers and remove common campaign/click identifiers.

The launcher rejects command-line overrides that can disable the sandbox, web security, certificate validation, site isolation, protected extensions or remote-debugging boundaries. Its own Windows process also enables stricter DLL search, image-load, legacy extension-point and dynamic-code mitigations before starting the browser runtime.

Ghosium does not claim anonymity. Websites intentionally visited by the user still receive normal web requests and may use their own cookies or fingerprinting techniques subject to browser controls and Ghosium filtering.

## Weak-PC optimization

Systems with **8 GiB RAM or less** automatically use Low Memory mode without reducing renderer/site isolation:

- systems with 4 GiB RAM or less use a 64 MiB disk-cache budget;
- systems above 4 GiB and up to 8 GiB use a 128 MiB disk-cache budget;
- background browser mode and unnecessary background networking stay disabled;
- no renderer-process cap is applied, so the browser keeps its normal process-isolation policy.

Users can force normal resource behavior with `--ghosium-balanced` or force the constrained cache profile with `--ghosium-low-memory`.

The canonical local profile path is `%LOCALAPPDATA%\Brendigo\Ghosium\User Data`. Existing profiles from earlier releases are detected and preserved so upgrading does not silently discard user data.

## Stable Release assets

A stable release intentionally attaches only:

- `Ghosium-Browser-Setup.exe`
- `Ghosium-Browser-Portable.exe`

GitHub automatically provides the matching **Source code (zip)** and **Source code (tar.gz)** archives for the release tag. Search/Store web source is therefore delivered through the source archive instead of adding extra release ZIP assets.

## Setup

The Setup EXE:

- runs per-user without administrator privileges
- shows the Brendigo BSD 3-Clause license before installation
- identifies Brendigo as Company/Publisher
- lets the user choose among 30 languages
- installs only Ghosium-owned shortcuts and Ghosium-owned support/legal URLs
- records the selected browser language
- includes all required third-party license material inside the installed application

## Portable

`Ghosium-Browser-Portable.exe` is a single release executable. It extracts the runtime to a temporary directory, keeps user data in `Ghosium-Portable-Data` beside the EXE, waits for Ghosium Browser to close and then removes the temporary runtime.

## Documentation

- `ARCHITECTURE.md` — runtime and trust boundaries
- `BUILDING.md` — build and verification
- `PRIVACY.md` — privacy model
- `SECURITY.md` — security policy
- `CONTRIBUTING.md` — contribution rules
- `CHANGELOG.md` — release history
- `docs/SEARCH.md` — Ghosium Search architecture
- `docs/STORE.md` — Ghosium Store architecture
- `docs/SHARED-HOSTING-SEARCH.md` — Search deployment
- `docs/SHARED-HOSTING-STORE.md` — Store deployment
- `docs/PERFORMANCE.md` — weak-PC strategy
- `docs/PRIVACY-HARDENING.md` — privacy rationale
- `docs/ENGINE-UPDATES.md` — engine update process
- `docs/RELEASE.md` — stable release procedure
- `THIRD_PARTY_NOTICES.md` — required third-party attribution

## License

Ghosium-authored source is licensed under the BSD 3-Clause License in `LICENSE`. Copyright holder/publisher: **Brendigo**. Third-party components keep their original licenses and required notices.